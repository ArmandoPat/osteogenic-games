# Runtime dependencies for the deployed comparison app ONLY.
# (The repo-root requirements.txt is the full notebook/ML analysis stack — not needed here.)
# Versions pinned to the environment the app was built and validated against.
streamlit==1.59.1
pandas==3.0.3
numpy==2.4.6
scikit-learn==1.9.0
scipy==1.18.0
altair>=5.0,<6.0

# Durable storage backend -- used ONLY when a database URL (BONEGRAFT_DB_URL / db_url) is
# configured, e.g. on Streamlit Community Cloud where the local filesystem is wiped on restart.
# With no database configured these are simply unused, so local/server behaviour is unchanged.
SQLAlchemy>=2.0,<3.0
psycopg2-binary>=2.9,<3.0
